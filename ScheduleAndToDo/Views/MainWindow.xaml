<Window x:Class="ScheduleAndToDo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ScheduleAndToDo.Views.CommonUI"
        xmlns:local="clr-namespace:ScheduleAndToDo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Schedule And ToDoList App" FontSize="30" HorizontalAlignment="Center"/>
        <!--スケジュール画面に遷移する-->
        <controls:NormarlButton Width="200"
                            Height="80"
                            FontSize="20"
                            ButtonText="スケジュールはこちら"
                            Margin="5"                                
                            Command="{Binding OpenScheduleCommand}"/>
        <!--ToDo画面に遷移する-->
        <controls:NormarlButton Width="200"
                            Height="80"
                            FontSize="20"
                            ButtonText="ToDoはこちら"
                            Margin="5"
                            Command="{Binding OpenToDoCommand}"/>
    </StackPanel>
</Window>
